Eclipse plugin providing a logger that prints log messages to the 
console view of the active eclipse workbench.

The AbstractWorkbenchDelegate initializes the according output stream
and registers it with an according log4j logger.